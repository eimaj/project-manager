# {{project_name}} — Last Session

> Per-session handoffs. Each /pm-end updates only its own session block.
> Each block is delimited by `<!-- PM:SESSION <id> START/END -->` so concurrent
> sessions on the same project never clobber each other's handoff.

_(no sessions captured yet — the first /pm-end writes a block here)_
